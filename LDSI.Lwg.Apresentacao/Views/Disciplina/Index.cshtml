@model IEnumerable<LDSI.Lwg.Apresentacao.Models.Disciplina>

@{
    ViewData["Title"] = "Disciplinas";
}

<div class="container col-md-10 mt-5">
    <p>
        <h3><a asp-action="Create" class="btn btn-success glyphicon glyphicon-plus"></a> Criar nova Disciplina</h3>
    </p>
    
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Nome)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Curso)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Nome)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Curso.CursoId)
                    </td>
                    <td>
                        <a class="btn btn-warning glyphicon glyphicon-pencil" asp-action="Edit" asp-route-id="@item.DisciplinaId"></a>
                        <a class="btn btn-info glyphicon glyphicon-search" asp-action="Details" asp-route-id="@item.DisciplinaId"></a>
                        <a class="btn btn-danger glyphicon glyphicon-trash" asp-action="Delete" asp-route-id="@item.DisciplinaId"></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>